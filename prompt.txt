# Task 2 — Prompt Improvement

## Bad Prompt
Make code.

## Improved Prompt
Write a JavaScript function called `greetUser` that:
- Accepts a string `name`
- Returns a friendly greeting message
- Validates that `name` is not empty
- Includes an example call at the end
